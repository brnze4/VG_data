'''This call will only scrape a URL for all images. It should
        Attempt to find high resolution versions, within reason
        if not, download the version shown
    It will not
        crawl page to page to gather images
'''
class single_url_scrape:
    def __init__(self):
      pass
